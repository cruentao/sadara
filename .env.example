# Root-level environment variables
# Each sub-project has its own .env.example:
#   - backend/.env.example
#   - frontend/.env.example
#
# Copy those files to .env in their respective directories before running.
